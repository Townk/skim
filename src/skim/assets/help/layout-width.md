# Layout Width

The total width of the generated keymap image.

**Default:** `800.0`

Larger values produce wider keymap images. The image height adjusts
automatically based on the layer layout or the number of layers for the
overview keymap image.
