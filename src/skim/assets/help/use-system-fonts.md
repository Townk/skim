# Use System Fonts

When enabled, the rendered keymap image uses system fonts instead of the
bundled fonts.

Enable this if you want the keymap to use fonts available on the viewing system
for SVG images, or in the host system for non-vector ones. Disable to ensure
consistent rendering across all platforms using the bundled fonts.
