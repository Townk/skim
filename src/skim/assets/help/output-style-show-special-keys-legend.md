# Show special keys legend

When enabled, per-layer images include a legend table of the macros and
tap-dances referenced on that layer, and the overview image includes the
full legend of every macro and tap-dance defined in the keymap.

Disable this option to omit those tables entirely — the layer images
will only show the keyboard, and the overview will only show the layer
grid.

Default: enabled.
