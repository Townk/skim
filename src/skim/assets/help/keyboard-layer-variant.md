# Layer Variant

An optional variant descriptor for the layer (e.g. `COLEMAK`, `QWERTY`).

When set, the variant is displayed as an additional text indicating the layer
variant on the overview keymap image.

Leave empty if the layer has no variant.
